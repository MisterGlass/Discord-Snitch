name: Test
on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_dispatch:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        docker build .
    - name: Run twigs
      run: |
        echo 1
