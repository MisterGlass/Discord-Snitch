name: Test
on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_dispatch:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run flake8
      run: |
        flake8 . --max-line-length=127
